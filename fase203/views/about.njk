{% extends "layout.njk" %}

{% block content %}

        <div id="wrapper">
            <img src={{data.avatarUrl}} alt="Gabriel Menegatto">
            <h1>{{data.username}}</h1>
            <h2>{{data.subName}}</h2>
            <p>{{data.userDesc}}</a></p>
        </div>

        <div id="links-footer" class="links">
            {%for link in data.link%}
                <a href={{link.url}} target="_blank">{{link.name}}</a>
            {%endfor%}
        </div>

{% endblock %}